<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<configuration>
  <context>
    <name>{{contextName}}</name>
    <desc>CASA Ready autogenerated context</desc>
    <inscope>true</inscope>
    <incregexes>{{targetUrl}}.*</incregexes>
    <tech>
      <include>Db</include>
      <include>Language</include>
      <include>OS</include>
      <include>SCM</include>
      <include>WS</include>
    </tech>
    <urlparser>
      <class>org.zaproxy.zap.model.StandardParameterParser</class>
      <config>{"kvps":"&amp;","kvs":"=","struct":[]}</config>
    </urlparser>
    <postparser>
      <class>org.zaproxy.zap.model.StandardParameterParser</class>
      <config>{"kvps":"&amp;","kvs":"=","struct":[]}</config>
    </postparser>
    <!--
      Form-based authentication (ZAP type=2). ZAP infers username/password
      FIELD NAMES from the key names in `loginbody` (e.g. given
      `email={%username%}&password={%password%}`, ZAP knows the field is
      called `email`). The casa-ready.config.js `usernameField` and
      `passwordField` settings are therefore for human reference / future
      auth-type variants — they are NOT injected here.

      `loggedin` is a regex matched against every response body. Avoid
      `<` or `&` in the pattern; the renderer will XML-escape them and
      ZAP will then see entity-encoded characters instead of literals.

      Known V1 limitation: this form-auth type does NOT work with JSON-body
      login endpoints (e.g. Supabase Auth's POST /auth/v1/token returning
      a JWT). Those need ZAP type=4 (script-based auth). Tracked as a
      follow-up in the project task list.
    -->
    <authentication>
      <type>2</type>
      <strategy>EACH_RESP</strategy>
      <pollurl></pollurl>
      <polldata></polldata>
      <pollheaders></pollheaders>
      <pollfreq>60</pollfreq>
      <pollunits>REQUESTS</pollunits>
      <loggedin>{{loggedInIndicator}}</loggedin>
      <form>
        <loginurl>{{loginUrl}}</loginurl>
        <loginbody>{{loginRequestBody}}</loginbody>
        <loginpageurl>{{loginUrl}}</loginpageurl>
      </form>
    </authentication>
    <users>
      <user>1;true;{{username}};2;{{username}}~{{password}}</user>
    </users>
    <forceduser>1</forceduser>
    <session>
      <type>0</type>
    </session>
    <authorization>
      <type>0</type>
      <basic>
        <header></header>
        <body></body>
        <logic>AND</logic>
        <code>-1</code>
      </basic>
    </authorization>
  </context>
</configuration>
